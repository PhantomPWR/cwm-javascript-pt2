console.log('src/index.js');

const Circle = require('./circle.js');

const circle = new Circle(1);
circle.draw(); // Circle with radius 1

// Terminal command for running this file:
// node es6-tooling/index.js

console.log('changed...');
